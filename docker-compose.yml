services:
  frontend:
    build: .
    container_name: frontend_despacho
    restart: unless-stopped
    ports:
      - '80:80'
    environment:
      - VITE_API_URL=http://localhost:8081
    networks:
      - frontend_net

networks:
  frontend_net:
    driver: bridge
